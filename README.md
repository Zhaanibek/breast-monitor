# BreastHealth Monitor

Система мониторинга температуры молочных желез с интеллектуальным анализом данных.

## 🎯 Описание

Прототип аппаратно-программной системы для неинвазивного мониторинга температуры молочных желез с целью раннего выявления температурных аномалий.

**Научное обоснование**: Онкологические процессы сопровождаются усилением ангиогенеза и метаболизма, что приводит к локальному повышению температуры на 1-3°C относительно здоровых тканей.

## 🏗️ Архитектура

```
breast_monitor/
├── backend/          # FastAPI сервер
│   ├── main.py       # Главное приложение
│   ├── config.py     # Конфигурация
│   ├── database.py   # PostgreSQL
│   ├── models/       # SQLAlchemy модели
│   ├── schemas/      # Pydantic схемы
│   ├── routers/      # API endpoints
│   └── services/     # Бизнес-логика
├── frontend/         # Веб-интерфейс
│   ├── index.html    # HTML
│   ├── styles.css    # CSS
│   └── app.js        # JavaScript
├── telegram_bot/     # Telegram бот
│   └── bot.py        # aiogram бот
├── arduino/         
│   
└── requirements.txt
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd breast_monitor
pip install -r requirements.txt
```

### 2. Настройка PostgreSQL

```bash
# Создать базу данных
createdb breast_monitor

# Или через psql
psql -U postgres -c "CREATE DATABASE breast_monitor;"
```

### 3. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env файл
```

### 4. Запуск сервера

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 5. Открыть веб-интерфейс

Перейдите на http://localhost:8000

### 6. Запуск Telegram бота (опционально)

```bash
cd telegram_bot
python bot.py
```

## 📊 Функциональность

### Режимы ввода данных

1. **Ручной ввод** - пользователь вводит температуры с 8 зон
2. **Загрузка термограмм** - анализ цветовых термоизображений
3. **ESP32 сенсоры** - автоматический сбор данных (будущее)

### API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/measurements` | POST | Ручной ввод температур |
| `/api/measurements` | GET | Список измерений |
| `/api/images/upload` | POST | Загрузка термограммы |
| `/api/analysis/current` | GET | Текущий анализ |
| `/api/analysis/history` | GET | История анализов |
| `/api/metrics` | GET | Метрики |

### Классификация рисков

| Уровень | Асимметрия | Макс. температура |
|---------|------------|-------------------|
| NORMAL | < 0.5°C | < 37.5°C |
| ELEVATED | 0.5-1.0°C | 37.5-38.0°C |
| HIGH | ≥ 1.0°C | ≥ 38.0°C |

## 📱 Telegram Bot

Команды:
- `/start` - Главное меню
- `/status` - Текущий статус
- `/input` - Ввод температур
- `/history` - История

## ⚠️ Ограничения

> **ВАЖНО**: Данная система является исследовательским прототипом и **НЕ является сертифицированным медицинским устройством**. Получаемые результаты предназначены исключительно для предварительного мониторинга и **НЕ заменяют консультацию врача**.

## 🛠️ Технологии

- **Backend**: Python 3.11, FastAPI, SQLAlchemy
- **Database**: sqlite (future:PostgreSQL) + asyncpg
- **Frontend**: HTML, CSS, JavaScript, Chart.js
- **Telegram**: aiogram 3.x
- **Hardware**: 

